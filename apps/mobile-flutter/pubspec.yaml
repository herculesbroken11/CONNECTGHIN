name: connectghin
description: ConnectGHIN — Where Golfers Connect
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  go_router: ^17.2.0
  dio: ^5.7.0
  flutter_secure_storage: ^10.0.0
  firebase_core: ^4.6.0
  firebase_messaging: ^16.1.3
  image_picker: ^1.1.2
  socket_io_client: ^3.1.4
  url_launcher: ^6.3.1
  intl: ^0.20.2
  geolocator: ^13.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
